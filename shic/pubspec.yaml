name: shic
description: A new Flutter package project.
version: 0.1.0
homepage: https://github.com/pedrox-hs/flutter_packages
repository: https://github.com/pedrox-hs/flutter_packages/tree/main/shic

environment:
  sdk: ^3.7.2
  flutter: ^3.29.2

resolution: workspace

dependencies:
  connectivity_plus: ^6.0.0
  flutter:
    sdk: flutter
  http: ^0.13.5
  meta: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_mock_web_server: ^1.0.0
  mocktail: ^1.0.2
  shared_lints: ^0.1.0

flutter: { }
